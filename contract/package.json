{
  "name": "shielded-vip-lounge-contract",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "compact": "compact compile src/TieredTokenGated.compact src/managed/TieredTokenGated",
    "build": "tsc --project tsconfig.build.json && cp -Rf ./src/managed ./dist/managed && cp ./src/TieredTokenGated.compact ./dist",
    "test": "vitest"
  },
  "dependencies": {
    "o1js": "^0.17.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
